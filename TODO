[]  Improve the file viewer so that the user can scroll through and view the whole file
[]  Allow user to insert and edit the file
[]  Add syntax highlighting
[]  Make a proper project directory structure with folders and everything
